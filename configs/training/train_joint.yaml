# @package _global_
training:
  stage: 3
  total_timesteps: 500000
  n_envs: 4
  learning_rate: 1.0e-4
  batch_size: 256
  n_epochs: 10
  gamma: 0.99
  checkpoint_interval: 50000
  eval_freq: 10000
  n_eval_episodes: 5
  log_dir: logs/
  checkpoint_dir: checkpoints/
  bot_joint_lr_factor: 0.1
